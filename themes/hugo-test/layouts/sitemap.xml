{{ printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
  xmlns:xhtml="http://www.w3.org/1999/xhtml">
  {{- range .Data.Pages }}
    {{- if and .Permalink (not .Params.seo.noIndex) }}
  <url>
    <loc>{{ .Permalink }}</loc>{{ if not .Lastmod.IsZero }}
    <lastmod>{{ safeHTML ( .Lastmod.Format "2006-01-02T15:04:05-07:00" ) }}</lastmod>{{ end }}{{ with .Params.seo.sitemap.changefreq }}
    <changefreq>{{ . }}</changefreq>{{ end }}{{ with .Params.seo.sitemap.priority }}
    <priority>{{ lang.NumFmt 1 . "-|.| " "|" }}</priority>{{ end }}
  </url>
    {{- end }}
  {{- end }}
</urlset>